name: "Test workflow"
on:
  push:
    branches: dev

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: setup-node
        uses: actions/setup-node@3

      - name: run test
        run: |
          yarn install --include=dev
          yarn test
